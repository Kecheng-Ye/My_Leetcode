# Sliding Window

* Template

```cpp
int left = 0, right = 0;

while (right < s.size()) {
    window.add(s[right]);
    right++;

    while (not valid) {
        window.remove(s[left]);
        left++;
    }

    // update window size
}
```
